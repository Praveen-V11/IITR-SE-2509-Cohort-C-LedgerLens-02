fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
openai==1.57.0
httpx==0.27.0
SQLAlchemy==2.0.35
prometheus-client==0.21.0
Pillow==10.4.0
python-multipart==0.0.9
streamlit==1.38.0
pandas==2.2.3
# Pinned explicitly: left unpinned, pip resolves the newest numpy/pyarrow
# (2.5.x / 25.x as of writing) which streamlit 1.38's Arrow-serialization
# path was never built against - st.data_editor segfaults (SIGSEGV, exit
# 139) when it converts the review-queue dataframe. These versions are
# contemporaneous with streamlit==1.38.0/pandas==2.2.3 and known-stable.
numpy==1.26.4
pyarrow==17.0.0
requests==2.32.3
